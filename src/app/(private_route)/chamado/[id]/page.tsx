import { useRouter } from "next/navigation";



type Props = {
  params: { id: string };
};
export default function NewChamadoPage({ params }: Props) {
  const { id } = params;

  return (
    <div>
      
    </div>
  );
}